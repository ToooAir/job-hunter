requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.44.0
openai>=1.30.0
langgraph>=0.4
qdrant-client>=1.9.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0
# Pinned together: streamlit 1.61.0 + starlette 1.4.0 crash the dashboard
# (GZipResponder.__init__() missing 'thread_minimum_size' — streamlit's gzip
# wrapper predates starlette making that kwarg required). 1.60.0 / 1.3.1 is the
# last verified-good pair; bump both in lockstep after testing the dashboard.
streamlit==1.60.0
starlette==1.3.1
pandas>=2.0.0
pyperclip>=1.8.2
python-docx>=1.1.0
reportlab>=4.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
